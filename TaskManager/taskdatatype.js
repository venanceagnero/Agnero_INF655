import React from 'react';

export default function Taskdatatype({ id, title, description }) {
    return ( <
        div > { ' ' } { id } { title } { description } { ' ' } <
        /div>
    );
}